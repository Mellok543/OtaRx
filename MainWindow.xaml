<Window x:Class="ElrsWifiBatchFlasher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ELRS RX WiFi Batch Flasher" Height="620" Width="980">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Firmware" Grid.Row="0" Margin="0,0,0,10">
            <DockPanel Margin="10">
                <Button Content="Choose .bin" Width="120" DockPanel.Dock="Right" Click="PickFirmware_Click"/>
                <TextBox x:Name="FirmwarePathTextBox" Margin="0,0,10,0"/>
            </DockPanel>
        </GroupBox>

        <GroupBox Header="RX WiFi AP settings" Grid.Row="1" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="SSID (or prefix):" Grid.Row="0" Grid.Column="0" Margin="0,0,10,8" VerticalAlignment="Center"/>
                <TextBox x:Name="SsidTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,18,8" Text="ExpressLRS RX"/>

                <CheckBox x:Name="PrefixCheckBox" Grid.Row="0" Grid.Column="2" Margin="0,0,10,8" Content="Treat as prefix" IsChecked="True" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="3" Margin="0,0,0,8"/>

                <TextBlock Text="Password:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBox x:Name="PasswordTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,18,0" Text="expresslrs"/>

                <TextBlock Text="After upload wait (ms):" Grid.Row="1" Grid.Column="2" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBox x:Name="AfterWaitTextBox" Grid.Row="1" Grid.Column="3" Text="8000"/>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="StartButton" Content="Start batch" Width="140" Margin="0,0,10,0" Click="Start_Click"/>
            <Button x:Name="StopButton" Content="Stop" Width="100" Click="Stop_Click" IsEnabled="False"/>
            <TextBlock Text="OK:" Margin="18,0,6,0" VerticalAlignment="Center"/>
            <TextBlock x:Name="OkCountText" Text="0" FontSize="16" VerticalAlignment="Center"/>
        </StackPanel>

        <GroupBox Header="Log" Grid.Row="3">
            <TextBox x:Name="LogTextBox" Margin="10" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>
        </GroupBox>
    </Grid>
</Window>